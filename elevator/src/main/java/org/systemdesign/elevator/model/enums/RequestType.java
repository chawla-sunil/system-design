package org.systemdesign.elevator.model.enums;

public enum RequestType {
    INTERNAL,   // button pressed inside the elevator (destination floor)
    EXTERNAL    // button pressed on a floor (up/down call)
}

